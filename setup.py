from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='build a project using covid19 dataset',
    author='rvm',
    license='MIT',
)
